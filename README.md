# IKT466 - Final Project

## Setup

To install all required packages, please run the following command in your terminal:

    pip install -r requirements.txt

## Run Program

To run the code, please run the following command in your terminal:

    python train.py

## Dataset

To create the datasets, please run the following commands in your terminal:

    make hex

Then to run the dataset generation:

    ./hex